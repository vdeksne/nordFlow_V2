# Neon Postgres (copy to `.env.local` and fill in credentials)
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/neondb?sslmode=require"

# Credential auth (login/register): HS256 session cookie; must be >= 32 chars.
# Generate: openssl rand -base64 32
AUTH_SECRET=""
